function Space({ height, width }) {
    return (
        <div
            style={{
                height: `${height}px`,
                width: `${width}px`,
            }}
        ></div>
    );
}

export default Space;
